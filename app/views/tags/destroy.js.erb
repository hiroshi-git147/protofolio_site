// modalを閉じる処理
$("#tagModal").modal("hide");
$("body").removeClass("modal-open");
$(".modal-backdrop").remove();

// 画面の書き換え
$('[data-model_name="tag"]').filter('[data-id="<%= @tag.id %>"]').remove();

